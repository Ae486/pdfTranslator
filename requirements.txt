# Web UI
gradio

# PDF to Markdown Conversion
magic-pdf

# Markdown to HTML Conversion
Markdown

# HTTP Requests for Translation API
requests

# PyMuPDF is a dependency of magic-pdf, so it's good to keep it explicit.
PyMuPDF 